ktor:
  application:
    modules:
      - mtg.app.ApplicationKt.module
  deployment:
    port: 8080

storage:
  jdbc:
    url: ${?DB_URL}
    user: ${?DB_USER}
    password: ${?DB_PASSWORD}
    driver: org.postgresql.Driver
    maxPoolSize: 10

firebase:
  projectId: ${?FIREBASE_PROJECT_ID}
